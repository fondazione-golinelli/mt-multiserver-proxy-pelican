name: Build and publish mt-multiserver-proxy Pelican image

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      force_build:
        description: "Force build even if this upstream commit is already published"
        type: boolean
        default: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}
  PROXY_UPSTREAM_REPO: fondazione-golinelli/mt-multiserver-proxy
  BRIDGE_UPSTREAM_REPO: fondazione-golinelli/mt-multiserver-proxy-pelican-bridge

jobs:
  check:
    runs-on: ubuntu-latest
    outputs:
      proxy_sha: ${{ steps.upstream.outputs.proxy_sha }}
      proxy_short_sha: ${{ steps.upstream.outputs.proxy_short_sha }}
      bridge_sha: ${{ steps.upstream.outputs.bridge_sha }}
      bridge_short_sha: ${{ steps.upstream.outputs.bridge_short_sha }}
      version_tag: ${{ steps.upstream.outputs.version_tag }}
      should_build: ${{ steps.upstream.outputs.should_build }}
    steps:
      - name: Query upstream main commits
        id: upstream
        run: |
          PROXY_DATA=$(curl -fsSL "https://api.github.com/repos/${{ env.PROXY_UPSTREAM_REPO }}/commits/main")
          PROXY_SHA=$(echo "$PROXY_DATA" | jq -r '.sha')
          PROXY_SHORT_SHA=$(printf '%s' "$PROXY_SHA" | cut -c1-12)
          PROXY_COMMIT_DATE=$(echo "$PROXY_DATA" | jq -r '.commit.committer.date')

          BRIDGE_DATA=$(curl -fsSL "https://api.github.com/repos/${{ env.BRIDGE_UPSTREAM_REPO }}/commits/main")
          BRIDGE_SHA=$(echo "$BRIDGE_DATA" | jq -r '.sha')
          BRIDGE_SHORT_SHA=$(printf '%s' "$BRIDGE_SHA" | cut -c1-12)
          BRIDGE_COMMIT_DATE=$(echo "$BRIDGE_DATA" | jq -r '.commit.committer.date')

          LATEST_COMMIT_DATE=$(printf '%s\n%s\n' "$PROXY_COMMIT_DATE" "$BRIDGE_COMMIT_DATE" | sort | tail -n1)
          TIMESTAMP=$(date -u -d "$LATEST_COMMIT_DATE" +%Y%m%d%H%M%S)
          VERSION_TAG="${TIMESTAMP}-${PROXY_SHORT_SHA}-${BRIDGE_SHORT_SHA}"

          echo "proxy_sha=${PROXY_SHA}" >> "$GITHUB_OUTPUT"
          echo "proxy_short_sha=${PROXY_SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "bridge_sha=${BRIDGE_SHA}" >> "$GITHUB_OUTPUT"
          echo "bridge_short_sha=${BRIDGE_SHORT_SHA}" >> "$GITHUB_OUTPUT"
          echo "version_tag=${VERSION_TAG}" >> "$GITHUB_OUTPUT"

          STATUS=$(curl -s -o /dev/null -w "%{http_code}" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "${{ env.REGISTRY }}/v2/${{ env.IMAGE_NAME }}/manifests/${VERSION_TAG}")

          if [ "$STATUS" = "200" ] && [ "${{ inputs.force_build || false }}" != "true" ]; then
            echo "should_build=false" >> "$GITHUB_OUTPUT"
          else
            echo "should_build=true" >> "$GITHUB_OUTPUT"
          fi

  build:
    runs-on: ubuntu-latest
    needs: check
    if: needs.check.outputs.should_build == 'true'
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          build-args: |
            MT_MULTISERVER_PROXY_REPO=${{ env.PROXY_UPSTREAM_REPO }}
            MT_MULTISERVER_PROXY_VERSION=${{ needs.check.outputs.proxy_sha }}
            MT_MULTISERVER_PROXY_PELICAN_BRIDGE_REPO=${{ env.BRIDGE_UPSTREAM_REPO }}
            MT_MULTISERVER_PROXY_PELICAN_BRIDGE_VERSION=${{ needs.check.outputs.bridge_sha }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:main
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.check.outputs.version_tag }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.check.outputs.proxy_short_sha }}-${{ needs.check.outputs.bridge_short_sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
